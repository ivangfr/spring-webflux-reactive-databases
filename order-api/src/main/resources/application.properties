server.port=9082
spring.application.name=order-api

spring.cassandra.keyspace-name=mycompany
spring.cassandra.contact-points=${CASSANDRA_HOST:localhost}:${CASSANDRA_PORT:9042}
spring.cassandra.local-datacenter=datacenter1
spring.cassandra.schema-action=CREATE_IF_NOT_EXISTS
spring.cassandra.request.timeout=10s

product-api.url=http://${PRODUCT_API_HOST:localhost}:${PRODUCT_API_PORT:9080}
customer-api.url=http://${CUSTOMER_API_HOST:localhost}:${CUSTOMER_API_PORT:9081}

management.endpoints.web.exposure.include=beans,env,health,info,metrics,mappings
management.endpoint.health.show-details=always

springdoc.show-actuator=true
springdoc.swagger-ui.groups-order=DESC
springdoc.swagger-ui.disable-swagger-default-url=true
